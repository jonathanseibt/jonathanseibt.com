{
  "name": "jonathanseibt.com",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "npm run check-types && npx next telemetry disable",
    "postinstall": "npx next telemetry disable && npx ts-typie",
    "check-types": "tsc --noEmit",
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "10.1.2",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "@types/react": "^17.0.3",
    "@types/react-dom": "^17.0.3",
    "typescript": "^4.2.3"
  }
}
